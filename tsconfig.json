{
    "compilerOptions": {
        "module": "none",
        "checkJs": true, //检查js语法
        "noLib": true, //不使用默认的库
        "moduleDetection": "force" // 强制每个文件都是模块
    },
    "include": ["./src/**/*", "./test/**/*", "./types/**/*"]
}
